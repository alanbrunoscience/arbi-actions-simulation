name: Build and Deploy

on:
  push:
    branches:
      - master

jobs:
  Deploy_application:
    runs-on: ubuntu-latest

    steps:
      - name: Print name branch
        run: "echo BASE_REF: ${{ github.event.pull_request.base.ref }}"